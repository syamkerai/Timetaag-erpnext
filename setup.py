from setuptools import setup, find_packages

setup(
    name="timetaag_sync_app",
    version="0.0.1",
    author="Syam",
    description="Custom app for syncing TimeTagg checkins with ERPNext",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
